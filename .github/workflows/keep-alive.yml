name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *' # 분마다 실행
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render server
        run: curl -s https://dff-server.onrender.com > /dev/null
